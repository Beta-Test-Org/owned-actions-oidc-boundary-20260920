name: Owned reusable caller with ID token

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  call:
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/reusable-oidc.yml
